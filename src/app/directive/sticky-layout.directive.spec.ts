import { StickyLayoutDirective } from './sticky-layout.directive';

describe('StickyLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
